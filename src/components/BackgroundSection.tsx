import React from "react";

type BackgroundSectionProps = React.HTMLAttributes<HTMLDivElement>;

const BackgroundSection = (props: BackgroundSectionProps) => <section {...props} />;

export default BackgroundSection;
